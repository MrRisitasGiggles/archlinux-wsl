wsl --set-default-version 2
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
wsl --install -d Arch

pacman-key --init
pacman-key --populate archlinux

pacman --sync --refresh --sysupgrade
pacman --sync base base-devel

passwd
useradd -m -G wheel -s /bin/bash <username>
passwd <username>

pacman --sync sudo

sudo pacman --sync extra/ruby-yard
sudo pacman --sync git openssh curl
sudo pacman --sync core/python3
sudo pacman --sync python-setuptools python-pip python-pipx sqlite xz tk
sudo pacman --sync extra/neovim extra/python-pynvim

sudo pacman --sync extra/nodejs
sudo pacman --sync extra/npm
sudo pacman --sync extra/tmux
sudo pacman --sync extra/btop
sudo pacman --sync extra/byobu

mkdir ~/Documents # Text, PDF, and office documents.
mkdir ~/Downloads # Temporary downloads.
mkdir ~/Projects # Source code, scripts, and project-specific files.
mkdir ~/bin # Personal scripts and executables.
mkdir ~/opt # Manually installed, third-party software.
mkdir ~/.config # User-specific configuration files (hidden).
mkdir ~/Pictures
mkdir ~/Videos
mkdir ~/Music
